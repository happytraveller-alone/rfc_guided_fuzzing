//----- (000000014004BD90) ----------------------------------------------------
ULONG __fastcall Ds_LdapUnbind(LDAP **a1)
{
  LDAP *v2; // rcx
  ULONG result; // eax

  v2 = *a1;
  if ( !v2 )
    return 0;
  result = ldap_unbind(v2);
  if ( !result )
  {
    *a1 = 0i64;
    return 0;
  }
  if ( result <= 0x61 )
    return LdapErrorMappingTable[result];
  return result;
}
// 14018CB70: using guessed type _DWORD LdapErrorMappingTable[100];
